name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: |
        git config --global user.email "phuocthien@outlook.com"
        git config --global user.name "Thien Nguyen"
        npm run myinstall
        npm run build-gh-page
